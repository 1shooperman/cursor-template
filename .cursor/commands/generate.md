# generate

**Before starting the generation:**
- Capture the current system time (i.e. if Linux/MacOS, run `date`) for report timestamping

Using the attached mcp-articles mcp server, generate an article template.